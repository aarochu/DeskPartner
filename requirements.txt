# DeskPartner harness (Track A). SDK + LeRobot live in their own envs per README.
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0
pillow>=10.0
anthropic>=0.40
google-generativeai>=0.8
# Person 5 — Rerun reBot port (p5_rerun_port)
rerun-sdk>=0.22
# Optional export niceties:
# pandas>=2.0
# pyarrow>=14.0
# Optional: pinocchio / motorbridge come from reBotArm_control_py (uv)
