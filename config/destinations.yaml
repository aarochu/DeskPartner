# Taught drop poses — outside camera zone. Jog once (P1), never guess.

destinations:
  trash:
    label: trash
    # Arm tip position at release (mm in arm base frame). Fill by jogging.
    xyz_mm: null            # e.g. [280, -180, 80]
    rpy_deg: null           # top-down release orientation
    container: "trash bin"
  pen_cup:
    label: pen_cup
    xyz_mm: null
    rpy_deg: null
    container: "wide-mouth jar (not a narrow cup — forgives ~3 cm)"
  tray:
    label: tray
    xyz_mm: null
    rpy_deg: null
    container: "tray for everything else"
  keep:
    label: keep
    action: never_touch
    note: "Judge phone / personal items — leave in place"

release:
  open_gripper_hold_s: 0.4
  retreat_up_mm: 40
