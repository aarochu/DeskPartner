# Copy to config/.env (never commit .env)

# Perception: vlm | cv
DESKPARTNER_PERCEPTION=vlm

# VLM providers (use at least one)
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional overrides
# DESKPARTNER_VLM_PROVIDER=anthropic
# DESKPARTNER_VLM_MODEL=claude-sonnet-4-20250514

# Modal (Track B — MolmoAct 2 train + serve)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
# MOLMOACT2_ENDPOINT=
# MOLMOACT2_BASE_CHECKPOINT=
